Place the product photo here as `Product.png`.

The dashboard and the "You're all set" screen load `/assets/Product.png`
at runtime. If the file is missing, the app shows an SVG fallback of the
band, so it still runs — but for the real look, drop the photo in here.
